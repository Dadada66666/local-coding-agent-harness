# Boolean configuration recovery fixture

The accepted values are specified in `CONTRACT.md`.

Run deterministic verification with fail-fast behavior using:

```bash
python3 -m unittest discover -v -f
```
