# Boolean configuration values

Accepted true values are `true`, `1`, `yes`, `on`, and `enabled`.

Accepted false values are `false`, `0`, `no`, `off`, and `disabled`.

Matching is case-insensitive and ignores surrounding whitespace. Other values are invalid.
